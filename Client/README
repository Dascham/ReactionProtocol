#An example of this kind of client could be an amazon server, making it the potential victim of a DDoS attack.
#Therefore this kind of client would need code, that can receive messages from an IDS signaling a DDoS attack.

#The Forwarder will be executed by FastNetMon when an attack is detected. Information will be passed onto the Linker, which in turn will build the signal-string and send to the assigned delegator.
